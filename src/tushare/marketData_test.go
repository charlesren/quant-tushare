package tushare

import (
	"testing"
)

func TestGetDaily(t *testing.T) {

}
func TestUpdateTradeCal(t *testing.T) {
	//db := config.GetDB()
	//defer db.Close()
	//var tradeCal TradeCal
	//today := time.Now().Format("20060102")
	//db.Where("cal_date = ?", today).Find(&tradeCal)
	//fmt.Println(tradeCal)
}
